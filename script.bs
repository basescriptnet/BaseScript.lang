let password = 'admin';
let message = switch* length of password {
    | 0: 'cannot send empty string';
    & 1:
    & 2:
    & 3:
    | 4: 'password is too short';
    | 5: 'password saved';
    default: 'password is too long';
};